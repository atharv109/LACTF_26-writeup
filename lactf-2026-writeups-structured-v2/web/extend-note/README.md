# extend-note

**Category:** Web  
**Primary source:** Discord export notes.

## Summary

The exported notes mark this as an **unintended** solve using the `blogler` instance for XSS.

## Main point

Instead of the intended route, solvers reused the XSS-capable environment from `blogler` and chained it into `extend-note`.

## Note

This page is intentionally short because the uploaded material only included the high-level unintended path, not the full intended exploit.
